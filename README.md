# omarchy-extra-apps

Extra applications for Omarchy

### REMEMBER:

- Discord is already pre-installed into Omarchy

- Check out Davinci Resolve fix in extra/

### INSTALLATION:

Install whiptail support:

```sudo pacman -S libnewt```

Make sure install.sh is executable:

```chmod +x install.sh```

Run:

```./install.sh```
